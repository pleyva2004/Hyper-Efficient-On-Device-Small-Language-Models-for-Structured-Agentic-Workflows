# Notes for Week 5 Meeting


In the current state of languge models we send a bunch of text on the iternet to the model to predict the next token throuh self-attention this token is the highest probable token of being predict because of the contined filters applied in the MLP layers and the self-attention blocks augmenting the context vector of each token. However we are focused not on predict the next token across a LARGE vocabulary instead we are focused on predicting the next MOVE / ACTION to take to get use closer to the goal



